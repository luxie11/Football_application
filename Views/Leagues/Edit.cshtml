
@{
    ViewData["Title"] = "Leagues";
}
@model FootballApplication.Models.Leagues
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor HttpContextAccessor

<div class="row">
    <div class="col-12 ">
        <form class="match-update" method="post" asp-controller="Leagues" asp-action="Edit">
            <div class="input-field">
                @Html.TextBox("Name", Model.Name, new { @placeholder = "Name" })
            </div>
            <div class="input-field">
                @Html.TextBox("NumberOfClubs", Model.NumberOfClubs, new { @placeholder = "Number of Clubs" })
            </div>
            <div class="input-field">
                @Html.TextBox("Founded", Model.Founded, new { @placeholder = "League founded" })
            </div>
            <div class="submit-button">
                <button type="submit" class="btn">
                    Submit
                </button>
            </div>
        </form>
    </div>
</div>
